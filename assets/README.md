# Hero media

Place the final cinematic cocktail photo or video in this folder. Then replace the `.hero-media` background in `styles.css` or add a `<video>` element inside `.hero-media` in `index.html`.
